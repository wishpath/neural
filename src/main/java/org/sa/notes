no need for values while initialising
sort pitctures by labels, add to separate lists by labels. then formulate batches by equal amounts of each of 10 digits.
for network improvement take equal amount of pictures for each digit. remember indexes so would always take other pictures.